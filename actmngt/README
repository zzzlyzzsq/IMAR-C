# List activities or preexisting BDDs:
  imarc list activities <bdd_name>
  imarc list bdds

# Predict activity
  imarc ar <video_path> <bdd_name>

# Add videos / create a new BDD / create a new activity:
  imar add /path/to/video1.avi ... /path/to/video_n.avi <bdd_name> <activity_name>
  imar add activity <activity_name> <bdd_name>
  imar add bdd <bdd_name>

# Effectuer les algorithmes d'apprentissage :
  imar compute <bdd_name>

# Suppression de BDD / activités :
  imar delete activity <activity_name> <bdd_name>
  imar delete bdd <bdd_name>

# Extract feature data
(supression de toutes les données sauf les vidéos et réextraction des STIPs)
  imar refresh <bdd_name>

# Transfering training data on the robot Nao
  imar transfer <bdd_name> <login> <robot IP> <password>